export enum Pages {
  home = '/',
  login = '/login',
  register = '/register',
  forgotPassword = '/forgot',
  resetPassword = '/reset',
  dashboard = '/dashboard',
  summarize = '/summarize',
  summarizeDetail = '/summarize/:id',
}
