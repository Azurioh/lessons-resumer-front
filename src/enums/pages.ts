export enum Pages {
  // Landing Pages
  home = '/',
  about = '/about',
  services = '/services',
  faq = '/faq',
  contact = '/contact',
  // Auth Pages
  login = '/login',
  register = '/register',
  forgotPassword = '/forgot',
  resetPassword = '/reset',
  // Summarize Pages
  dashboard = '/dashboard',
  summarize = '/summarize',
  summarizeDetail = '/summarize/:id',
}
